
[system_cmd]
LOCK_SCREEN     = sudo loginctl lock-sessions
SHUT_DOWN_NOW   = sudo loginctl shutdown now

LOCK_USER   	= sudo passwd -l 
UNLOCK_USER 	= sudo passwd -u 
CHANGE_PASSWD   = sudo passwd
ADD_USER        = sudo adduser -m -s /bin/sh

